epoch: 600

LearningRate:
  base_lr: 0.01
  schedulers:
  - !YOLOv5LRDecay
    max_epochs: 600
    min_lr_ratio: 0.01
  - !LinearWarmup
    start_factor: 0.
    epochs: 3

OptimizerBuilder:
  optimizer:
    type: Momentum
    momentum: 0.937
    use_nesterov: True
  regularizer:
    factor: 0.001
    type: L2
  clip_grad_by_norm: 10.
